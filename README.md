# AI-CSRF

这是一个用于自动化修复 CSRF 风险的项目。

目标：

1. 接收前后端仓库地址。
2. 分析 CSRF 防护缺口。
3. 生成修复改动与测试。
4. 自动提交 PR，并在满足策略时合并。

项目文档见 `docs/` 目录。

目前完成了的：目录、文档和进度机制已就绪，run 命令支持前后端仓库参数、地址校验、run-id 和 dry-run 计划输出，并提供了 PowerShell 入口与使用文档
另外，当前暂不直接修改目标仓库代码，主要是为了先验证流程，避免误改并确保每一步可追踪、可回滚，后续再逐步完成CSRF相关的风险识别和自动修复。
